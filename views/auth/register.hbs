<h1>Register your account</h1>

{{#if errorMessages}}
    <div class="warning">
        Error: Please make sure to correct the following issue(s) when trying again:
        <ul>
            {{#each errorMessages}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
{{/if}}

<form action="/register" method="POST">
    <label for="username">Username:</label><input type="text" name="username" required>
    <label for="password">Password:</label><input type="password" name="password" required>
    <label for="confirmation">Password (Repeat):</label><input type="password" name="confirmation" required>
    <input type="submit" value="Register Account">
</form>

<p>Already registered? <a href="/login">Login here</a></p>